server:
  port: 8081

spring:
  application:
    name: splitmate-user-service

  datasource:
    url: jdbc:mysql://localhost:3306/splitmate?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: toor
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

security:
  secret: THIS_IS_A_SECRET_KEY_FOR_SPLITMATE_256_BIT_SECRET
  expiration-ms: 36000000
